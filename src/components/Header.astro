---
export interface Props {
  className?: string;
}

const { className = '' } = Astro.props as Props;
---
<header class={`fixed left-0 top-0 right-0 w-full py-2 z-20 backdrop-blur ${className}`}>
  <slot />
</header>
